<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="en">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta charset="UTF-8" />

    <title>POD Status Update</title>

    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background-color: #f4f7fa;
            margin: 0;
            padding: 0;
        }

        .email-wrapper {
            width: 100%;
            padding: 30px 0;
            display: flex;
            justify-content: center;
            background-color: #f4f7fa;
        }

        .email-container {
            width: 650px;
            background: #ffffff;
            padding: 25px 35px;
            border-radius: 12px;
            box-shadow: 0px 4px 12px rgba(0,0,0,0.08);
        }

        .header {
            text-align: center;
            margin-bottom: 25px;
        }

        .header h2 {
            font-size: 24px;
            color: #333333;
            margin: 0;
            padding-bottom: 8px;
        }

        .status-badge {
            display: inline-block;
            padding: 6px 14px;
            font-size: 14px;
            font-weight: 600;
            border-radius: 20px;
            color: #fff;
        }

        .status-approved {
            background-color: #28a745;
        }

        .status-rejected {
            background-color: #dc3545;
        }

        .info-box {
            background: #f8fafc;
            border-left: 4px solid #3b82f6;
            padding: 15px 18px;
            margin-top: 20px;
            margin-bottom: 25px;
            border-radius: 6px;
            font-size: 15px;
            color: #444;
        }

        .label {
            font-weight: bold;
            color: #333;
            width: 160px;
            display: inline-block;
        }

        .footer {
            text-align: center;
            margin-top: 25px;
            font-size: 13px;
            color: #888;
        }
    </style>
</head>

<body>

<div class="email-wrapper">
    <div class="email-container">

        <!-- Header Title -->
        <div class="header">
            <h2>Purchase Order Status Update</h2>

            #if($status == "Approved")
                <span class="status-badge status-approved">APPROVED</span>
            #else
                <span class="status-badge status-rejected">REJECTED</span>
            #end
        </div>

        <!-- Message -->
        <p style="font-size:15px; color:#333;">
            Hello Team,<br /><br />
            The status of the purchase order has been updated. Please find the details below:
        </p>

        <!-- Info Box -->
        <div class="info-box">
            <p><span class="label">Purchase ID:</span> ${purchaseId}</p>
            <p><span class="label">Approved / Rejected By:</span> ${currentApprover}</p>
            <p><span class="label">Approval Time:</span> ${approvalTime}</p>
            <p><span class="label">Total Amount:</span> ₹${amount}</p>
            <p><span class="label">Status:</span> ${status}</p>

            #if($reason)
            <p><span class="label">Reason:</span> ${reason}</p>
            #end
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>RoomBillz Notification Service</p>
            <p>If you did not expect this update, please contact the support team.</p>
        </div>

    </div>
</div>

</body>
</html>
